package com.spectra.consumer.Utils;

import android.content.Context;


import com.google.android.exoplayer2.Player;
import com.google.android.exoplayer2.ui.PlayerView;

public class VideoUtils {


    public static Player getPalyer(PlayerView playerView, Context mContetx, String videoSource) {
//
//        player = new VideoPlayer(playerView, mContetx, videoSource, mContetx);
//
//        // Used to pause/resume player on incoming calls
//        mAudioManager = (AudioManager) mContetx.getSystemService(Context.AUDIO_SERVICE);
//
//        // Used to hide/show player controller views
//        playerView.setControllerVisibilityListener(visibility -> {
//            if (player.isLock())
//                playerView.hideController();
//            back.setVisibility(visibility == View.VISIBLE && !player.isLock() ? View.VISIBLE : View.GONE);
//        });
//
//        // Optional setting
//        playerView.getSubtitleView().setVisibility(View.GONE);
//        player.seekToOnDoubleTap();
        return null;
    }
}
